/**
 * src/main/__tests__/sidecar-manager.test.ts
 *
 * SidecarManager 유닛 테스트 (Phase 0 placeholder).
 * Phase 1에서 실제 테스트 케이스를 추가한다.
 */

describe('SidecarManager', () => {
  it('placeholder: test framework is working', () => {
    expect(true).toBe(true)
  })
})
