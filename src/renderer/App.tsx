/**
 * src/renderer/App.tsx
 *
 * React 루트 컴포넌트.
 * Phase 4에서 EditorPage, SettingsPage 등 라우팅이 추가된다.
 *
 * @see docs/architecture/04-editor-ui.md §2
 */

import React from 'react'

/**
 * 전체 애플리케이션의 최상위 컴포넌트.
 * 현재는 Phase 0 placeholder — "빈 창" 상태.
 */
export function App(): React.ReactElement {
  return (
    <div id="app-root">
      {/* Phase 4: 라우터 및 레이아웃 추가 */}
    </div>
  )
}
