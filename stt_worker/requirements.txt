# Luciper STT Worker — Python Dependencies
# Phase 0: 의존성 목록 확정 (실제 설치는 Phase 0-8 가상환경에서 수행)

# Core STT
faster-whisper>=1.0.2

# Audio capture
sounddevice>=0.4.6
numpy>=1.26.0

# Data validation (JSON schema)
pydantic>=2.6.0

# Linting & formatting (dev only)
ruff>=0.4.0
black>=24.3.0

# Testing
pytest>=8.1.1
pytest-cov>=5.0.0
pytest-mock>=3.14.0
